import { NotFoundSection } from './NotFound.styled'

export const NotFound = () => {
  return (
    <NotFoundSection>
        <h1>404</h1>
        <h2>Not Found</h2>
        <p>Ops... Wasn't possible to find the route, try again later!</p>
    </NotFoundSection>
  )
}
